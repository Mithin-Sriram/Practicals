
Here’s a **clean, exam-ready Java program** for:

> **TCP Server that receives a request, converts lowercase → uppercase, and sends response back**

---

# ✅ **Concept**

### 🔹 What this program does

- Client sends a **string**
    
- Server:
    
    - Receives request
        
    - Processes it (convert to uppercase)
        
    - Sends response back
        

Uses **Transmission Control Protocol**:

- Reliable
    
- Connection-oriented
    

---

### 🔹 Working Flow

1. Server starts and listens
    
2. Client connects
    
3. Client sends string
    
4. Server converts → uppercase
    
5. Server sends result back
    
6. Client displays output
    

---

# 💻 **Java Program**

## 🔹 Server Program

```java
import java.io.*;
import java.net.*;

public class UppercaseServer {
    public static void main(String[] args) {
        try {
            ServerSocket serverSocket = new ServerSocket(5000);
            System.out.println("Server waiting...");

            Socket socket = serverSocket.accept();
            System.out.println("Client connected");

            BufferedReader in = new BufferedReader(
                    new InputStreamReader(socket.getInputStream()));

            PrintWriter out = new PrintWriter(socket.getOutputStream(), true);

            String request;

            while ((request = in.readLine()) != null) {
                System.out.println("Received: " + request);

                // Convert to uppercase
                String response = request.toUpperCase();

                // Send back
                out.println(response);
            }

            socket.close();
            serverSocket.close();

        } catch (Exception e) {
            System.out.println(e);
        }
    }
}
```

---

## 🔹 Client Program

```java
import java.io.*;
import java.net.*;
import java.util.Scanner;

public class UppercaseClient {
    public static void main(String[] args) {
        try {
            Socket socket = new Socket("localhost", 5000);

            BufferedReader in = new BufferedReader(
                    new InputStreamReader(socket.getInputStream()));

            PrintWriter out = new PrintWriter(socket.getOutputStream(), true);

            Scanner sc = new Scanner(System.in);

            System.out.print("Enter string: ");
            String msg = sc.nextLine();

            // Send request
            out.println(msg);

            // Receive response
            String response = in.readLine();
            System.out.println("Response from server: " + response);

            socket.close();
            sc.close();

        } catch (Exception e) {
            System.out.println(e);
        }
    }
}
```

---

# ▶️ **How to Execute**

### Step 1: Save files

- `UppercaseServer.java`
    
- `UppercaseClient.java`
    

---

### Step 2: Compile

```bash
javac UppercaseServer.java
javac UppercaseClient.java
```

---

### Step 3: Run Server FIRST

```bash
java UppercaseServer
```

---

### Step 4: Run Client

```bash
java UppercaseClient
```

---

# 📌 **Sample Output**

### Server:

```text
Server waiting...
Client connected
Received: hello world
```

---

### Client:

```text
Enter string: hello world
Response from server: HELLO WORLD
```

---

# 🧠 **Explanation (Simple)**

### Server:

- Waits for connection
    
- Reads string
    
- Uses `toUpperCase()`
    
- Sends result
    

### Client:

- Sends user input
    
- Receives processed data
    
- Displays output
    

---

# 🎯 **Important Exam Points**

- Uses TCP sockets
    
- Server processes request
    
- String manipulation (`toUpperCase`)
    
- Continuous communication possible
    

---

# 🎓 **Viva Questions**

### 1. What does server do here?

Processes request and sends response.

---

### 2. Why TCP is used?

Reliable communication.

---

### 3. What method converts to uppercase?

`toUpperCase()`

---

### 4. What is request-response model?

Client sends request, server replies.

---

### 5. Can this handle multiple clients?

No (single client unless threads added)

---

If you want, I can also give:

- Multi-client server (threaded)
    
- Reverse string / palindrome version
    
- Diagram for exam writing